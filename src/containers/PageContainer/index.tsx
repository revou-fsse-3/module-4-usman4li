import { Card } from "../../Components"

const PageContainer = () => {

    return(
        <Card border={false} className={''}>
            <Card border>
                Halaman
            </Card>
        </Card>
    )
}

export default PageContainer